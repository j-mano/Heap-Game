using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SysSpecReqCheck : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Miniumum planed sys req is quadtreaded cpu, 4 gb systemram and intel hd gpu (hd 515 (6 gen intel cpus))
    // Clearifikation of minimum rew = 30 fps with minimum graphics.
}
